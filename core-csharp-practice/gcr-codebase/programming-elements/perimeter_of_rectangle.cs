using System;
class perimeter_of_rectangle{
	static void Main(){
		double length = 2.5; //Length of Rectangle
		double width = 4.5; //Width of Rectangle
		double perimeter = 2 * (length + width); //Perimeter of Rectangle
		Console.WriteLine(perimeter);
	}
}